# 탄스택 쿼리 기본 이해

# 기본 이해

- 쿼리 (Query): 데이터 담긴 상자
    - 서버에서 받아온 데이터(data), 해당 데이터에 대한 상태 정보(마지막으로 업데이트된 시간(dataUpdatedAt) 등) 가지고 있음
    - 쿼리키가 동일하면 쿼리 인스턴스는 단 하나만 존재
- 옵저버 : 쿼리 구독 장치
    - `useQuery`를 호출할 때마다 생성
    - 쿼리 키에 해당하는 쿼리를 구독함
    - 하나의 쿼리를 구독하는 여러 옵저버가 있을 수 있음
    - 쿼리의 데이터가 변하면 컴포넌트에게 알려서 리랜더링 발생시킴
    - 각 옵저버는 자신만의 `staleTime`이나 `enabled` 같은 설정을 가질 수 있음
        
        → 쿼리의 마지막으로 업데이트된 시간(dataUpdatedAt) 을 보고 개별적으로 stale, fresh 판단 
        
        ```jsx
        // A 옵저버
        useQuery({
          queryKey: ['todos'], // 쿼리 키가 같으면 같은 캐싱 데이터 
          staleTime: 0,
        })
        
        // B 옵저버
        useQuery({
          queryKey: ['todos'],  // 쿼리 키가 같으면 같은 캐싱 데이터 
          staleTime: 1000 * 60 * 5,
        })
        ```
        
        - 캐시 데이터의 "fresh/stale 판단"은 각 observer 기준
        isStale = now > dataUpdatedAt + staleTime
        - 따라서 캐시 데이터라도 A 옵저버에게는 `stale` 일 수 있고 B 옵저버에게는 `fresh`일 수 있다.

# 캐시 상태
<img width="1625" height="707" alt="image" src="https://github.com/user-attachments/assets/fd905660-d241-47dc-95f7-75b0c8816c7f" />

- **fetching** : 데이터가 불러와 지고 있는 상태
- **fresh** : 데이터가 신선한 상태
    
    → staleTime 이후에는 stale 상태로 판단된다
    
- stale : 데이터를 신뢰할 수 없는(= 상한) 상태
    
    → 특정 조건이 만족되면 refetching을 진행한다
    
- **inactive**: 브라우저 화면에 이 캐시 데이터를 활용하는 컴포넌트가 하나도 없는 상태
    - 데이터의 fresh, stale 상태와 관계없이 화면에 이 캐시를 활용하는 컴포넌트가 없다면 다 inactive 상태가 된다
    
    → gcTime 이후에 해당 데이터는 메모리에서 제거(deleted 상태)가 된다
    
- deleted : 데이터가 메모리에서 아예 제거된 상태

## 1) stale 상태 데이터

### staleTime

: 캐시를 stale 상태로 판단하기까지의 시간 → 기본값이 ‘0’초 

: 각 쿼리는 데이터의 `dataUpdatedAt` 시간을 기준으로 현재 시간이 `dataUpdatedAt` + `staleTime` 이내라면 fresh 데이터로 판단

: 모든 쿼리에 기본적으로 적용되는 staleTime은 ‘0초’

⇒ 따로 staleTIme 설정 안하면 가져온 데이터는 바로 stale 상태로 판단 된다

### stale 데이터가 refetch 되는 조건

: **데이터가 stale상태라고 무조건 refetch 되는게 아니다**

: 데이터가 **stale 상태**이면서 아래 조건 중 하나 만족해야 refetch 발생함

1. 마운트 (**`refetchOnMount)`**
    
    : 이 캐시 데이터를 사용하는 컴포넌트가 마운트 되었을 떄
    
2. windowFocus(`refetchOnWindowFocus`)
    
    :  사용자가 이 탭에 다시 돌아왔을 떄 
    
3. reconnect (**`refetchOnReconnect`)**
    
    : 인터넷 연결 끊겼다가 다시 연결되었을 때 
    
4. interval (**`refetchInterval`)**
    
    : 특정 시기를 주기로 
    

→ 옵션으로 on/off 제어할 수 있음 

일반적으로는 마운트만 true로 설정하고 나머지는 false로 설정함

```jsx
useQuery({
  queryFn: fetchTodos,
  queryKey: ['todos'],
  
  **refetchOnMount:false, // 기본값 true
  refetchOnWindowFocus: false, // 기본값 true
  refetchOnReconnect:false // 기본값 true
  refetchInterval: 500 // 500ms마다 refetch 발생(stale 상태라면)**
})
```

### stale 데이터의 활용(=staleTime과 gcTime의 관계)

캐시된 데이터가 stale된 상태라고 해도 

**일단 stale 데이터를 활용해 빠르게 화면을 랜더링** 하고 

이후에 최신 데이터 받아오면 **최신 데이터로 교체해 재랜더링 해준다**

단) **staleTime 보다 gcTime 이 더 커야** 서버에서 fresh한 데이터를 가져오는 동안 캐시 데이터를 보여줄 수 있음. 탄스택쿼리는 기본적으로 staleTime이 0초, gcTime이 5분으로 설정되어 있어서 이게 기본동작인 것 처럼 보임

⇒ 로딩 화면을 최소한으로 보여주기 위한 탄스택 쿼리의 기능

## 2) inactvie 상태 데이터

: inactive 상태의 캐시 데이터가 많은건 메모리 낭비로 이어질 수 있음 

### gcTime

: garbage collections time (쓰레기 치우는 시간)

: i**nactive 상태의 캐시 데이터는 gcTime 뒤에는 Deleted 상태**가 되어 메모리 상에서 아예 사라진다

⇒ active 상태의 데이터의 경우에는 gcTime 적용 안됨

(ex: 화면에 보여지고 있는 상태의 데이터의 경우 gcTime을 아무리 짧게 줘도 메모리에서 사라지지 않음. 그 데이터를 사용하는 컴포넌트가 하나도 없어진 다음에 gcTime 타이머 시작)

: 사용자가 자주 방문하지 않는 페이지의 데이터는 짧은 GC Time을 설정하여 메모리에서 빠르게 제거

: 기본값 300000ms(=5분) 

```jsx
useQuery({
  queryFn: fetchTodos,
  queryKey: ['todos'],
  
  **gcTime:5000, // 기본값 300000ms(5분)**
})
```

# 탄스택 훅 비교

- 조회용
    - **useQuery**
    - useSuspenseQuery  : useQuery와 같은 동작인데 로딩을 Suspense로 던짐
    
    
    > 💡
    
    > useQuery vs useSuspenseQuery
    
    > - `useQuery`: “로딩/에러 상태를 **값으로** 돌려줌”
        - `isLoading`, `isFetching`, `error` 등을 컴포넌트에서 조건 분기
    > - `useSuspenseQuery`: “로딩/에러를 **던져서**(throw) 상위가 처리”
        - 로딩 → `<Suspense fallback>`
        - 에러 → `<ErrorBoundary fallbackRender>`
    
    > ⇒ **UI 처리(로딩중, 에러 발생시) 책임**이 컴포넌트 내부(상태에 따라 조건부 랜더링)인지, 상위 컴포넌트(fallback)인지가 달라짐

    > - useQueries : 여러 쿼리를 병렬적으로 조회한다. 각 쿼리별로 에러 처리함
    
    > : 가져온 데이터를 queryKey 기준으로 캐싱한다
    
- 쓰기(생성/수정/삭제)용
    - useMutation
    
    : 서버에 쓰기 요청 보냄 → 서버 상태 변경됨 → 클라이언트에서 사용중인 캐시에게 이 사실을 notify해야함 → 요청 완료 전/후에 실행되는 onError, onMutate,onSettled 에서 이런 처리를 함(캐시 데이터 stale상태로 변경, refetch 발생 등)
    
    - onMutate: 요쳥(mutateFn)이 **실행**되었을 때 실행되는 콜백함수
    - onSettled : 요청이 **종료**되었을 때 실행되는 콜백함수
    - onSuccess : 요청이 **성공**했을 때 실행되는 콜백함수
    - onError : 요청이 **실패**했을 때 실행되는 콜백함수
    
    : 조회쿼리와 달리 응답 결과를 캐싱하지 않음.
    

## 캐시 조작 메서드

QueryClient 가 제공하는 메서드

- 캐시 데이터 무효화 `invalidateQueries`
    
    :  queryKey에 해당하는 모든 쿼리의 staleTime 설정을 무시하고 강제로 캐시를 stale 상태로 만든다
    
    :  **현재 화면에 떠있는 쿼리(active상태)는 바로 refetch** 발생됨
    
    `queryClient.invalidateQueries({ queryKey })`
    
- 재요청 강제 `refetchQueries`
    
    : queryKey에 해당하는 쿼리 중 활성(active) 상태의 쿼리들의 경우 재요청 
    
    `queryClient.refetchQueries({ queryKey, type: "active" | "inactive" | "all" })`
    


> 💡

> invalidateQueries vs refetchQueries

> - **`invalidateQueries`**: 해당 쿼리키에 해당하는 캐시를 무효화(stale 상태로) 해서 다음 트리거 *때*  refetch 하게 만든다.
> - **`refetchQueries`**: 조건에 맞는 쿼리를 **즉시 refetch** 한다
> <aside>

- 미리 가져오기 : `prefetchQuery`
    
    : 데이터를 가져와 캐시에 저장
    
    - useQuery랑 다른 점
        - 데이터 반환하지 않음(조용히 캐시에 반영)
        - 에러 발생해도 그냥 넘어감
- 캐시 데이터 직접 설정 `setQueryData`
    
    : 서버 거치지 않고 클라이언트에서 직접 캐시 수정
    
    - sse, 웹소켓 등 실시간 데이터 주입에서 사용
    - 낙관적 업데이트에서 사용
    
    `queryClient.setQueryData(queryKey, updater)`
    
- 쿼리 키에 해당하는 캐시 조회 `getQueryData`
    
    `queryClient.getQueryData(queryKey)`
    
- 캐시 메모리 상에서 제거 `removeQueries`
    
    `queryClient.removeQueries({ queryKey })`
    
- 캐시 메모리에서 제거하고 다시 요청 `resetQueries`
    
    : remove + feretch 기능
    

## 유용한 기능들

- select 옵션
    
    : 전체 받아온 데이터를 가공하거나, 필요한 데이터만 뽑아야 할 때
    
    ❗ **캐시에는 원본 데이터가 저장됨**
    
    → 이 옵션을 사용하는 옵저버에게만 가공해서 반환. 다른 옵저버들에게는 원본 캐시 데이터 반환
    
    - 전체 데이터 중 일부만 뽑아 와 사용할 때
    - DTO → ViewModel 변환할 떄
    
    ```jsx
    useQuery({
      queryKey: ['menus'],
      queryFn: fetchMenus,
      select: (data) => data.filter(menu => menu.price > 10000),
    });
    // 데이터 중 가격이 만원 이상인 데이터만 필터링
    ```
    
- placeholderData
    
    : 로딩중에 보여줄 데이터 지정
    
    : 인자로 previous 전달해줌
    
    → 같은 컴포넌트에서 직전에 사용하던 data이전 렌더에서 이 useQuery가 사용하던 데이터
    
    ❗ 동일한 쿼리키의 이전 값 아님
    
    ```jsx
    useQuery({
      queryKey: ['report', date],
      queryFn: fetchReport,
      placeholderData: previous => previous,
    });
    ```
    

---

## 참고자료

https://velog.io/@gominzip/TanStack-Query%EB%8A%94-%EC%96%B4%EB%96%BB%EA%B2%8C-%EB%8D%B0%EC%9D%B4%ED%84%B0%EB%A5%BC-%EC%BA%90%EC%8B%B1%ED%95%A0%EA%B9%8C

https://www.youtube.com/watch?v=MArE6Hy371c